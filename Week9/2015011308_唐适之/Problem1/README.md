folder "a" is "all the source files along with the makefile"
folder "b" is "a library file (maze.a) and the header file (BaseRouter.h), the source files of OptRouter and makefile"
folder "src" compiles into the two above