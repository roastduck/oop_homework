#include "KMPBase.h"
#include "KMPQuery.h"

// Modify the code to accept command-line arguments
int main()
{
	// Code for command-line arguments ...
	
    KMPQuery query(/* The arguments need to be provided */);

    // The following code should NOT be changed.
    query.match();
    query.printMatches();

    return 0;
}
